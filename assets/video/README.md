# Demo video

Drop the final demo video here as `rtla-assistant-demo.mp4`.

- **Max length: 2:00** (hard requirement — submission is blocked without a video).
- Follow the script in [`docs/07-demo-script.md`](../../docs/07-demo-script.md).
- Build the video around the **hero moment**: the agent flags a missing clock gate with an exact RTL line pointer.

> `.mp4`/`.mov` files are gitignored (large binary). Keep the master in OneDrive and upload the final to the Innovation Studio submission page.
